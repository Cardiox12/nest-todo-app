# Use postgres/example user/password credentials
version: '3.1'

services:
  db:
    image: postgres
    restart: always
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: "tony"
      POSTGRES_PASSWORD: "1234"
      POSTGRES_DB: "todo"
  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080
# volumes:
#   db: 
#     driver: local